from replit import db


def watch_the_base():
  # del db["students"]
  print(db.keys())
  # print (db['statuses']['1'].keys())
  # for key in db.keys():
  #   if key != 'statuses':
  #     del db[key]
  
  